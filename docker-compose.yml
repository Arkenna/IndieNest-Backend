services:
  indienest-backend:
    image: indienest-backend:1.0.0
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=${SPRING_PROFILES_ACTIVE}
      - INDIENEST_MYSQL_PWD=${INDIENEST_MYSQL_PWD}